#include<iostream>

using namespace std;

int main(){
    int i = 10;
    while (i > 0)
    {
        printf("%d ",i);
        i -= 1;
    }
    //for loop, do-while loop 的寫法都跟Java壹模壹樣(其實三個都一樣拉);
    
}